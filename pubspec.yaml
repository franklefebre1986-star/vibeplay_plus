name: mijn_iptv_app
description: VibePlay+ IPTV
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=2.18.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.6
  # voeg andere packages die je gebruikt toe (video_player, shared_preferences) indien nodig
  shared_preferences: ^2.1.1
  video_player: ^2.5.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/logos.json
    - assets/logos/    # zorg dat alle logo-bestanden onder assets/logos/ staan (recursief)
